<?xml version="1.0" encoding="utf-8"?>
<!-- attention (the web app's coral): a hub whose identity changed -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/r_coral_soft" />
    <stroke android:width="1dp" android:color="@color/r_coral" />
    <corners android:radius="16dp" />
</shape>
