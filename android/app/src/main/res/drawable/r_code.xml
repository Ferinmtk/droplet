<?xml version="1.0" encoding="utf-8"?>
<!-- the pairing code, like the web app's .card.attn: an aqua ring on a soft fill -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/r_accent_soft" />
    <stroke android:width="1.5dp" android:color="@color/r_accent" />
    <corners android:radius="20dp" />
</shape>
